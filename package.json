{
    "scripts": {
        "test": "jest",
        "coverage": "jest --coverage --coverageReporters=text-lcov | coveralls"
    },
    "devDependencies": {
        "coveralls": "^3.0.14",
        "jest": "^23.1.0"
    }
}
